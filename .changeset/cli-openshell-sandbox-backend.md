---
"@getoma/cli": patch
---

bridge daemon: optional OpenShell sandbox backend for relayed sandbox ops (opt-in via `oma bridge setup` or `BRIDGE_SANDBOX_BACKEND=openshell`); `oma bridge status` now reports the active backend.
